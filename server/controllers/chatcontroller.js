// controllers/chatController.js

/**
 * Placeholder for future chat-related features,
 * like saving chat history, loading old messages, etc.
 */

const getChatHistory = (req, res) => {
  // To be implemented later
  return res.status(200).json({ messages: [] });
};

module.exports = {
  getChatHistory,
};
